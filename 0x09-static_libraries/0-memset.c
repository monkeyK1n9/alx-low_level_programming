#include "main.h"

/**
 * _memset - function
 * @s: string
 * @b: character to check
 * @n: integer
 * Return: pointer to character
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
